// The inspections landing shows the Defect Settings list. The sidebar routes
// here via /inspections/defect-settings; this re-export keeps /inspections working.
export { default } from './defect-settings/page';
